<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= content_for(:title) || "Inventory Management" %></title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
  <%= stylesheet_link_tag 'application', 'data-turbo-track': 'reload' %>
  <%= javascript_importmap_tags %>
</head>
<body class="bg-gray-100">

  <!-- Navigation Bar -->
  <nav class="bg-blue-600 shadow-lg">
    <div class="max-w-7xl mx-auto px-6 py-4 flex justify-between items-center">
      <!-- Left side navigation -->
      <div class="flex space-x-4">
        <% if current_user %>
          <%= link_to "Dashboard", dashboard_path, class: "bg-gray-200 hover:bg-gray-300 text-black px-4 py-2 rounded-lg shadow" %>
          <%= link_to "Products", products_path, class: "bg-gray-200 hover:bg-gray-300 text-black px-4 py-2 rounded-lg shadow" %>
          <%= link_to "Categories", categories_path, class: "bg-gray-200 hover:bg-gray-300 text-black px-4 py-2 rounded-lg shadow" %>
        <% end %>
      </div>

      <!-- Right side authentication -->
      <div class="flex space-x-4">
        <% if current_user %>
          <%= button_to "Logout", logout_path, method: :delete, class: "bg-red-500 hover:bg-red-600 text-white px-4 py-2 rounded-lg shadow" %>
        <% else %>
          <%= link_to "Login", login_path, class: "bg-green-500 hover:bg-green-600 text-white px-4 py-2 rounded-lg shadow" %>
          <%= link_to "Register", signup_path, class: "bg-blue-500 hover:bg-blue-600 text-white px-4 py-2 rounded-lg shadow" %>
        <% end %>    
      </div>
    </div>
  </nav>

  <!-- Content -->
  <main class="max-w-4xl mx-auto mt-10 px-6">
    <%= yield %>
  </main>

</body>
</html>
