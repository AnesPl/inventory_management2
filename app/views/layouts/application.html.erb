<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= content_for(:title) || "Inventory Management" %></title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag 'application', 'data-turbo-track': 'reload' %>
  <%= javascript_importmap_tags %>
</head>
<body class="bg-gray-50">

  <!-- Navigation Bar -->
  <nav class="bg-blue-600 shadow-lg">
    <div class="max-w-7xl mx-auto px-6 py-4 flex justify-between items-center">
      <div class="text-white font-bold text-xl">
        <%= link_to 'Inventory Management', root_path, class: 'text-white hover:text-gray-200' %>
      </div>
      <div class="flex space-x-6">
        <%= link_to 'Dashboard', dashboard_path, class: 'text-white hover:text-gray-200' %>
        <%= link_to 'Products', products_path, class: 'text-white hover:text-gray-200' %>
        <%= link_to 'Logout', logout_path, class: 'text-white hover:text-gray-200' %>
      </div>
    </div>
  </nav>

  <!-- Content -->
  <main class="container mx-auto mt-10 px-6">
    <%= yield %>
  </main>

</body>
</html>
